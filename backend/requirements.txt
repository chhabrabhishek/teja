# Dabble backend — Python 3.12 or 3.13 recommended
Django>=5.1,<6.0
django-ninja>=1.3,<2.0
pydantic[email]>=2.9
psycopg[binary]>=3.2
PyJWT[crypto]>=2.9
cryptography>=43.0
python-dotenv>=1.0
boto3>=1.35
requests>=2.32
httpx[http2]>=0.27    # APNs uses HTTP/2
django-cors-headers>=4.4
redis>=5.0            # optional: rate limiting + counters
openai>=1.50          # only used by the offline prompt generator
gunicorn>=23.0
uvicorn[standard]>=0.30
